enum Role { elder, caregiver, pending }
enum Diseases { diabetes, hypertension}
enum TaskType { medicine, doctor, custom }
enum CardLayoutType {
  vertical,   // ภาพบน
  horizontal, // ภาพซ้าย
  imageBottom // ภาพล่าง
}
enum FoodAlarmType { breakfast, lunch, dinner}